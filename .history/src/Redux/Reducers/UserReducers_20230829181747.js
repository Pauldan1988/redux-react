//* Boilerplate of Reducer Redux
const initialState = {
    userData: {},
}

export const UserReducers = (state = initialState, action) => {

    switch(action.type) {
        case "STORE_USER_DATA":
            return
    }

}